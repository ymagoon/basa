<% title 'Courses' %>
<div class="course-information">
  <div class="container">
    <h1 class="text-center">List of all courses</h1>
  </div>
</div>
<div class="wrapper">
  <div class="better-container">
    <div class="main-page">
      <%= render 'filters' %>
      <%#= link_to 'Add Course', new_course_path %>
      <div class="course-content">
        <!-- <div class="course-content-head">
          <h2 id="results"><%#= (@courses.count) %> <%#= "Result".pluralize(@courses.count) %> Found</h2>
          <%#= render 'shared/dropdown' %>
        </div> -->
        <%= render 'shared/course_list', courses: @courses %>
      </div>
    </div>
  </div>
</div>

      <button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#courses">
        Add Course
      </button>

<%= render 'shared/modal', title: 'Add a New Course', partial: 'courses/form', course: @course, subjects: @filter_subjects, addresses: @filter_addresses %>


