
<%= form_tag '/courses', method: 'get', remote: true, :id => 'location-filter' do %>
  <% @venues.each do |venue| %>
    <%= check_box_tag "address[#{venue[0]}]", 1, false, class: 'location-filter' %>
    <%= label_tag venue[0], venue[0] %>
  <% end %>
  <%= submit_tag 'submit', :id => 'submit' %>
<% end %>
