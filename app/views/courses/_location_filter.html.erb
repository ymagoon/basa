
<%= form_tag('/courses',method: 'get', class: 'test') do %>
  <% @venues.each do |venue| %>
    <%= check_box_tag("address[#{venue[0]}]") %>
    <%= label_tag(venue[0], venue[0]) %>
  <% end %>
  <%= submit_tag('submit') %>
<% end %>
